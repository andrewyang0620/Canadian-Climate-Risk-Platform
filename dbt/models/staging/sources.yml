version: 2

sources:
  - name: core
    database: "{{ env_var('SNOWFLAKE_DATABASE', 'CLIMATE_RISK') }}"
    schema: CORE
    description: >
      Curated warehouse source tables loaded from canonical ADLS Gold outputs.
    tables: []